# SERVER PORT 설정
server:
  port: 8080


# SPRING NAME
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/srcshbk
    username: srhan
    password: srhan@0310
    driver-class-name: org.postgresql.Driver
  mybatis:
      mapper-locations: classpath:/mapper/**/*.xml
      type-aliases-package: com.cshbk.cshmint
      configuration:
        map-underscore-to-camel-case: true
  application:
    name: cshmint
management:
  endpoints:
    web:
      exposure:
        include: health